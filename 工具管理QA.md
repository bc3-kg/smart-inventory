以下、確認事項ごとに**現時点で確定している内容**、**未確定である内容**、および**推奨案**を明確に分けて回答する。
なお、ZAICO API上は在庫データに `state`、`place`、`code`、追加項目として扱える `optional_attributes` が存在し、在庫更新は `PUT /api/v1/inventories/{id}`、出庫は `POST /api/v1/packing_slips/`、入庫は `POST /api/v1/purchases/`、取引先一覧は `GET /api/v1/customers/` で扱う仕様である。 ([ZaiCodev][1])

---

## 1. 誤操作対策は、操作②以外のすべてで行う認識でよいか

**回答**
現時点のSRS方針としては、**その認識で問題ない**と整理するのが妥当である。
すなわち、**参照専用である操作②（バーコード確認）を除き、状態変更を伴う操作①・③・④・⑤・⑥・⑦のすべてに誤操作対策を適用する**案を正式案とするのが望ましい。

**理由**
誤操作対策の目的は、同一シリアルNo.に対する短時間での重複更新防止であり、参照のみの操作②には適用意義が薄い。一方、他の操作はすべてローカル状態またはZAICO在庫状態を書き換えるため、対象外とする合理性が乏しい。

**推奨記載案**
SRSには以下のように明記すべきである。

> 誤操作対策は、状態変更を伴う操作（操作①、③、④、⑤、⑥、⑦）を対象とし、参照専用の操作②には適用しない。

---

## 2. 「ステータス」「箱番」は在庫データの追加項目内に追加し管理してよいか

**回答**
**「ステータス」は追加項目で管理すべきではない。**
**「箱番」は追加項目で管理して問題ない。**

**理由**
本件では既に、

* 業務ステータス → ZAICO `state`
* 所在 → ZAICO `place`
  と定義済みである。したがって、ステータスをさらに追加項目でも持つと、**二重管理**となり認識齟齬の原因になる。
  一方、箱番はZAICO標準の固定項目ではないため、ZAICO側へ持たせる場合は `optional_attributes` で保持するのが自然である。ZAICO在庫検索でも追加項目名・値を条件に指定できる仕様がある。 ([ZaiCodev][1])

**結論**

* ステータス：**不可推奨**。`state` に格納する。
* 箱番：**可**。必要に応じて追加項目で管理する。

**推奨案**

* ZAICO：

  * `state` = 業務ステータス
  * `place` = 所在
  * `code` = シリアルNo.
  * `optional_attributes` = 箱番、再研磨回数、補助情報
* ローカルSQLite：すべての最新状態を保持

---

## 3. 誤操作対策は、在庫データの更新日時とオペレーション日時を比較して判断でよいか

**回答**
**そのままでは不十分であり、原則としては不適切である。**

**理由**
ZAICO在庫データの `updated_at` は、在庫レコードが最後に更新された日時であり、**どの操作で更新されたか**、**同一操作の再実行か**、**ローカル未同期の操作があるか**を判別できない。ZAICO在庫データには `created_at` や `updated_at` が存在するが、これだけで業務上の誤操作判定を完結させるのは情報不足である。 ([ZaiCodev][1])

**問題点**

* 操作③の直後に操作④を行う場合、更新日時だけでは正常遷移か誤操作か区別できない
* 任意保存や補助項目更新でも `updated_at` が変わり得る
* ローカル先行保存・後同期方式と整合しない

**推奨案**
誤操作対策は、**ローカル SQLite の操作履歴**を基準とし、以下で判定するのが望ましい。

> 同一シリアルNo.かつ同一操作コードについて、最後の成功登録時刻から30分以内である場合に警告を表示する。

**補助的運用**
ZAICO `updated_at` は、あくまで**同期差分確認**や**競合検知の補助情報**として使うのが適切である。

---

## 4. 操作⑦の次操作は、操作③を行う認識でよいか

**回答**
**その認識のままでは問題がある。操作⑦の次操作は、原則として操作①であるべきである。**

**理由**
操作③は「払出中 → 受取済」の遷移であり、既に払出処理が完了している個体に対する現場受領登録である。
一方、操作⑦後の状態は「再研済」であり、まだ払出中ではない。したがって、状態遷移上、操作⑦の次に直接操作③へ進むのは不整合である。

**正しい遷移**

* 操作⑦：再研中 → 再研済
* 次操作候補：**操作① 払出登録**
* その後：操作③ 受領登録

**推奨記載案**

> 操作⑦完了後の次操作は、原則として操作①（払出登録）とする。操作③（受領登録）は、操作①により払出中となった後にのみ実行可能とする。

---

## 5. 操作別に画面を変える場合、メニュー画面を追加して遷移する認識でよいか

**回答**
**その認識で問題ない。むしろ推奨案である。**

**理由**
本件では操作ごとに入力項目、遷移条件、同期API、バリデーションが異なるため、1画面にすべてを集約すると誤操作や認識混乱が生じやすい。
そのため、**メニュー画面から操作対象画面へ遷移する構成**は、操作明確性・教育容易性・保守性の観点で適切である。

**推奨構成**

* メニュー画面

  * 払出登録
  * バーコード確認
  * 受領登録
  * 使用済登録
  * 廃却登録
  * 再研受入登録
  * 再研完了登録
* 各操作専用画面

**補足**
将来的に、現場端末向け・工具室端末向け・研磨室端末向けに表示メニューをロール別制御する案も有効である。

---

## 6. QRコード読取後、読取値を画面に残せない件について、案①・案②のどちらがよいか

**回答**
**案②を推奨する。**

**理由**
案①は、既存の「工具明細の管理番号」を実質的にQRコード表示として流用する案であるが、利用者視点では「今読んだ値」が直感的に見えにくく、画面下部スクロールが必要になる懸念も明示されている。
一方、案②は、スキャン結果の入力欄自体はクリアしつつ、**確定読取値を別表示領域に残す**方式であり、現在のReTool上の制約を回避しながら、利用者の確認要求にも応えやすい。

**推奨仕様**

* 入力欄「スキャン結果」：制御用、一時入力欄
* 表示欄「読取済シリアルNo.」または「読取済QRコード」：確定値表示欄
* 規定文字数到達時に

  1. 在庫検索
  2. 表示欄へ反映
  3. 入力欄をクリア

**表示名の推奨**
「QRコード」は媒体名であり、業務上の値としては**シリアルNo.**が正式名称であるため、画面ラベルは次が望ましい。

* `読取済シリアルNo.`
* 必要に応じて補足として `（QRコード読取値）`

---

## 7. ステータスをプルダウン選択できるようにする場合、変更可能ステータスを制限する認識でよいか

**回答**
**その認識で問題ない。むしろ必須である。**

**理由**
本件では業務遷移が明確に定義されているため、ZAICOに存在する全ステータスを無制限に選択可能にすると、誤遷移を許容してしまう。
したがって、プルダウンを実装する場合でも、**現在状態に応じて遷移可能な候補のみ表示**するか、少なくとも**選択後に遷移妥当性チェック**を行う必要がある。

**推奨順位**

1. **最推奨**：ステータスは自動設定とし、プルダウン非表示または読取専用表示
2. 次善策：変更可能候補のみプルダウン表示
3. 非推奨：全候補表示 + 後続チェック

**結論**
仕様確定案としては、以前のSRS方針どおり、**ステータスは操作ごとに自動設定**とするのが最も安全である。

---

## 8. 工具明細に、ステータス、箱番、払出先を表示すべきか

**回答**
**表示すべきである。**

**理由**
今回の要件では、工具明細は単なる在庫一覧ではなく、**個体管理の確認単位**となる。現行表示の「管理番号（QRコード）、物品名、数量、再研磨回数」に加えて、今回追加された業務判断項目である「ステータス」「箱番」「払出先」を表示しないと、現場利用時に確認性が不足する。

ただし、数量については本件が「1個体=1在庫レコード」を前提としているため、明細単位では実質的に常に1となる可能性が高い。そのため、表示優先度は下がる。

**推奨表示項目**

* シリアルNo.
* 物品名
* 業務ステータス
* 所在
* 払出先
* 箱番
* 再研磨回数
* 同期状態

**補足**
一覧の横幅が不足する場合は、

* 一覧：シリアルNo.、物品名、ステータス、所在、払出先
* 詳細表示：箱番、再研磨回数、同期状態、備考
  という分割も適切である。

---

## 9. 操作⑤で廃却とした場合、当該在庫データ数量は0に変更する認識でよいか

### 代替物品を購入した場合、QRコードは新規採番か

**回答**
この点は、**現時点では業務確定事項としては未確定**と明示すべきである。
ただし、**推奨案**は明確である。

### 9-1. 廃却時に数量を0へ変更するか

**推奨案**としては、**数量を0に変更しない**方がよい。

**理由**
本件では、数量変動を伴うZAICO API利用は、

* 操作①：出庫
* 操作⑥：入庫
  の各1回に限定する方針である。
  ここで操作⑤でも数量を0に変更すると、数量変動ルールが二重化し、整合が崩れる。加えて、ZAICO出庫・入庫APIは数量増減を伴う専用機能として定義されている。 ([ZaiCodev][1])

したがって、廃却は**数量変更ではなく、状態変更**として扱うのが一貫的である。

**推奨仕様**

* 操作⑤では

  * `state = 廃却`
  * `place = 工具室`
* 数量は変更しない
* 必要に応じて「廃却フラグ」相当の業務ステータスで識別する

### 9-2. 代替物品購入時のQRコード

**推奨案**としては、**新規採番**で問題ない。むしろその方が望ましい。

**理由**
本件は個体管理であり、1個体 = 1シリアルNo. = 1ZAICO在庫レコードが前提である。
したがって、廃却された個体と新規購入個体を同一QRコードで扱うと、個体履歴の連続性が破綻する。別個体である以上、**新規シリアルNo.を採番する**のが適切である。

**結論**

* 廃却個体：既存シリアルNo.のまま `state=廃却`
* 代替購入個体：**新規シリアルNo.で別在庫レコードを作成**

---

## 推奨する最終回答方針の要約

今回の確認事項に対する、仕様側の回答としては次の整理が最も整合的である。

* 誤操作対策は操作②以外の全操作に適用する
* ステータスは `state`、箱番は追加項目で管理する
* 誤操作判定はZAICO更新日時ではなく、ローカル操作履歴を基準とする
* 操作⑦後の次操作は操作①であり、操作③ではない
* 画面はメニュー画面から操作別画面へ遷移する構成で問題ない
* QR読取値の表示は案②を推奨する
* ステータス候補は制限必須であり、可能なら自動設定とする
* 工具明細にはステータス、箱番、払出先を表示する
* 廃却時の数量0変更は未確定だが、推奨は「数量変更なし・状態変更のみ」
* 代替購入品のQRコードは新規採番を推奨する

